const ColorQuote = () => {
var quote = ["I think we all need to be able to dream in all colors."]
const consoleQuote = quote.map((quote) => {
    return (quote)
})

console.log(consoleQuote)
}
export default ColorQuote